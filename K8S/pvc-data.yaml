apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aidocsearch-data
  namespace: aidocsearch
  labels:
    app: aidocsearch
    component: data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  # Using default storage class (hostPath on Minikube)
